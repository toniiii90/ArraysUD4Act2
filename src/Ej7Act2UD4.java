public class Ej7Act2UD4 {
    public static void main(String[] args) {
        double[] array={};
        double total = 0.0;

        for (double valor:array)
            total += valor;

        System.out.println("El total es: " + total);


        // a) El programa compilará, pero hay que tener en cuenta que el array está vacío.
        // b) El array no tiene elementos.
        // c) El programa se ejecutará sin errores. No ejecutará el bucle.
        // d) Imprimirá lo siguiente: "El total es: 0.0". La variable 'total' se inicializa en 0.0 y se imprime al final.



    }
}
